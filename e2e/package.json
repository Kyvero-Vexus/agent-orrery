{
  "name": "agent-orrery-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright E2E tests for Agent Orrery Web Dashboard (Epic 4)",
  "scripts": {
    "test": "playwright test",
    "test:trace": "playwright test --trace on",
    "report": "playwright show-report ../test-results/e2e-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
